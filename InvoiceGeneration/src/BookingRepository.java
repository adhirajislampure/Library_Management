public class BookingRepository implements BookingRepository {
    private Map<Long,Booking> bookings=new HashMap<>();

    public List<Booking> findByCustomerSessionId(Long SessionId){
        return
    }

}
